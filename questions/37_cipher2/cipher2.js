const decypher = (str => {
  //YOUR CODE HERE
  const retStr = '';
  const pointer = 0;
  for(let i = 0; i < str.length;)
  const numSeq = Array.from(str).reduce(acc, (elem) => {
    if (typeof elem === 'number')
  }, '')
  String.fromCharCode()
  return retStr;
});
module.exports = { decypher };
